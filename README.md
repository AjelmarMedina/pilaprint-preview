
  # PILAPRINT

  This is a code bundle for PILAPRINT. The original project is available at https://www.figma.com/design/lO4NsxdG3osM5713S9Di11/PILAPRINT.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  